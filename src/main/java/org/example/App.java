package org.example;

/**
 * Hello world!
 */
public class App {
//    public static void main(String[] args) {
////        List<User> list = new ArrayList<>();
////
////        for (int i = 0; i < 2; i++) {
////            User user = User.builder().name("xie" + i).ids(Stream.of("foo", "bar" + i)
////                    .collect(Collectors.toList())).build();
////            list.add(user);
////        }
////        list.forEach(System.out::println);
////        list.stream().map(user -> user.getName()).forEach(System.out::println);
////        list.stream().map(user -> user.getIds()).forEach(System.out::println);
////        list.stream().flatMap(user -> user.getIds().stream()).distinct().forEach(System.out::println);
//
////        list.stream().flatMap(user -> user.getIds().stream()).forEach(System.out::println);
//
//        // 构建set集合
////        list.stream().flatMap(user -> user.getIds().stream()).collect(Collectors.toSet()).forEach(System.out::println);
//
//        // 构建map集合
////        Map<String, Integer> map = list.stream().collect(Collectors.toMap(User::getName, user -> user.getIds().size()));
////        System.out.println(map);
//
//        // mybatis-plus代码生成器
//    }



}
